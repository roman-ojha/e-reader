input addUserInput {
  username: String!
  first_name: String
  last_name: String
  email: String!
  password: String!
  role: Role!
}

type Mutation {
  addUser(addUserCriteria: addUserInput!): User
  addAuthor(userId: Int!): Author
}
